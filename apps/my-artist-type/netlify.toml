[build]
  command = "cd ../.. && bun install && cd apps/my-artist-type && bun run build"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/vite-plugin-tanstack-start"
