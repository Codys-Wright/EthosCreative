[build]
  command = "cd ../.. && bun install && cd apps/my-artist-type && bun run build"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "20"

[dev]
  command = "bun --bun vite dev"
  targetPort = 3000
  autoLaunch = false
