// Common types used throughout the application

/**
 * Artist Type data structure used on the landing page
 */
export interface LandingPageArtistType {
  id: string;
  name: string;
  shortDescription: string;
  image: string;
  order: number;
}

// Add more types as needed
